/**
 * Firebase Service - Retired
 * World Brain functionality now relies exclusively on Local Laboratory Storage.
 */

export const initWorldBrain = async (addLog: (entry: any) => void) => {
  return null;
};

export const runDiagnosticPing = async (addLog: (entry: any) => void) => {
  return;
};

export const getDb = () => null;
